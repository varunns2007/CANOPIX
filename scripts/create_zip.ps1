const fs = require('fs');
const path = require('path');
const { execSync } = require('child_process');
const archiver = null; // Let's use standard zip via PowerShell script or Node archiver / tar

// We can create a powershell script and run it
